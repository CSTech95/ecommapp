name: Test Auth

on:
    push:
        branches:
            - main
        paths:
            - "auth_service/**"

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: cd auth_service && npm install && npm run test:ci
